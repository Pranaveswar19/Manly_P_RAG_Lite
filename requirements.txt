# === LlamaIndex core + integrations (versions intentionally differ across pkgs) ===
llama-index-core==0.14.3
llama-index-llms-openai==0.5.6
llama-index-vector-stores-faiss==0.5.1
llama-index-embeddings-huggingface==0.6.1

# Reranker (used via `from llama_index.core.postprocessor import SentenceTransformerRerank`)
sentence-transformers==3.0.1

# Vector search backend
faiss-cpu==1.8.0.post1

# UI
streamlit==1.38.0

# OpenAI client + utilities
openai>=1,<3
python-dotenv==1.0.1
requests==2.32.3